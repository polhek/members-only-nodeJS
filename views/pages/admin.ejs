<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include('../partials/head'); %>
  </head>
  <body class="bg-gray-200 font-mono">
    <header
      class="w-screen bg-blue-700 flex justify-between font-mono items-center"
    >
      <%- include('../partials/header'); %>
    </header>

    <div class="flex justify-center">
      <div class="rounded flex flex-col w-1/3 mt-12 bg-white shadow p-4">
        <h1 class="text-xl mb-5">To become admin of this page you need to insert admin password bellow!</h1>
        <div>
          <form action="" method="POST" class="flex flex-col">
            <label for="admin_password">Admin password:</label>
            <input
              name="admin_password"
              placeholder="Insert members only password here!"
              type="password"
              class="mb-5 border p-3"
              required
            />

            <button
              class="bg-red-500 hover:bg-red-700 text-white font-bold py-2 px-4 rounded"
            >
              Become admin!
            </button>
          </form>




          <% if (error) { %>
              <p class="text-red-700 text-xl font-bold mt-3"> <%= error %></p>
            <% } %>

        </div>
      </div>
    </div>
  </body>
</html>
