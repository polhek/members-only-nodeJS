<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include('../partials/head'); %>
  </head>
  <body class="bg-gray-200 font-mono">
    <header
      class="w-screen bg-blue-700 flex justify-between font-mono items-center"
    >
      <%- include('../partials/header'); %>
    </header>

    <div class="flex justify-center">
      <div class="rounded flex flex-col w-1/3 mt-12 bg-white shadow p-4">
        <h1 class="text-xl mb-5">Wanna become a member? Sign up now!</h1>
        <div>
          <form action="" method="POST" class="flex flex-col">
            <label for="first_name">First name:</label>
            <input
              name="first_name"
              placeholder="Insert your first name here..."
              type="text"
              class="mb-5 border p-3"
              required
            />
            <label for="last_name">Last name:</label>
            <input
              name="last_name"
              placeholder="Insert your last name here..."
              type="text"
              class="mb-5 p-3 border"
              required
            />
            <label for="username">Username:</label>
            <input
              name="username"
              placeholder="Insert your  username here..."
              type="text"
              class="mb-5 p-3 border"
              required
            />
            <label for="email">Email:</label>
            <input
              name="email"
              placeholder="Insert your email here..."
              type="email"
              class="mb-5 p-3 border"
              autocomplete="email"
              required
            />
            <label for="password">Password:</label>
            <input
              name="password"
              type="password"
              class="mb-5 border p-3"
              required
              autocomplete="new-password"

            />
            <label for="passwordConfirmation">Confirm password:</label>
            <input
              name="passwordConfirmation"
              type="password"
              class="mb-5 border p-3"
              autocomplete="new-password"
              required
            />
            <button
              class="bg-red-500 hover:bg-red-700 text-white font-bold py-2 px-4 rounded"
            >
              Sign Up
            </button>
          </form>


            <% for (var i = 0; i < errors.length;  i++ ) { %>
              <p class="text-red-700 text-xl font-bold mt-3"> <%= errors[i].msg %></p>
              <% } %>

        </div>
      </div>
    </div>
  </body>
</html>
